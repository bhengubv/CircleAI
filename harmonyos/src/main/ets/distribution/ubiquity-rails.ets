/**
 * distribution/ubiquity-rails
 *
 * ArkTS for HarmonyOS. Ported from the TypeScript source of the same module by
 * `harmonyos/tools/gen_arkts.py`: ArkTS is TypeScript without the dynamic
 * parts, so the logic is shared rather than retyped, and what differs is the
 * platform - Node's filesystem, paths and cryptography are reached here through
 * `src/main/ets/platform.ets`, which is over `@ohos.*`.
 *
 * Edit the TypeScript source, not this file.
 */

// The constant ubiquity rails - the decisions that turn a substrate into
// something a person can actually get, use and leave.
//
// GENERATED by typescript/tools/gen_rails_ts.py from the table in
// python/tools/gen_rails.py. Do not edit this file; edit the table, which both
// this and the Python port are generated from, so a decision cannot say one
// thing in one language and something else in another.
//
// WHY THESE ARE CONSTANTS AND NOT CONFIGURATION. Every one is a decision
// somebody has to be able to defend. A regulator approval that a config file
// could flip is not an approval; a cost ceiling a deployment can raise is not a
// ceiling. They are `readonly` and frozen for the same reason.
//
// MONEY IS IN MINOR UNITS, as integers. JavaScript has one number type and it
// is a double: 0.1 + 0.2 is not 0.3, and a total that does not match the sum of
// its parts is the bug that follows. Cents throughout, formatted at the edge.

/**
 * OEMs with a preload agreement. Mid-tier and below, because that is what
 * people here actually buy.
 */
export interface OemPreloadCatalog {
  readonly partners: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const oemPreloadCatalog: OemPreloadCatalog = Object.freeze({
  partners: Object.freeze(["Tecno", "Itel", "Samsung mid-tier", "Xiaomi", "Huawei"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultOemPreloadCatalog = oemPreloadCatalog;
export type IOemPreloadCatalog = OemPreloadCatalog;

/**
 * Carriers with a preload agreement.
 */
export interface CarrierPreloadCatalog {
  readonly carriers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const carrierPreloadCatalog: CarrierPreloadCatalog = Object.freeze({
  carriers: Object.freeze(["MTN", "Vodacom", "Cell C", "Telkom", "Safaricom", "Airtel"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCarrierPreloadCatalog = carrierPreloadCatalog;
export type ICarrierPreloadCatalog = CarrierPreloadCatalog;

/**
 * Where a device with no installable package goes instead.
 */
export interface PwaFallback {
  readonly pwaUrl: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const pwaFallback: PwaFallback = Object.freeze({
  pwaUrl: "https://app.circle.ai",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultPwaFallback = pwaFallback;
export type IPwaFallback = PwaFallback;

/**
 * Formats somebody can be handed directly, without a store.
 */
export interface SideloadChannel {
  readonly formats: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const sideloadChannel: SideloadChannel = Object.freeze({
  formats: Object.freeze(["APK", "IPA", "MSIX"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultSideloadChannel = sideloadChannel;
export type ISideloadChannel = SideloadChannel;

/**
 * Package managers the Linux build fans out to.
 */
export interface LinuxRepoFanout {
  readonly repos: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const linuxRepoFanout: LinuxRepoFanout = Object.freeze({
  repos: Object.freeze(["apt", "yum", "pacman", "brew", "flatpak", "snap"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultLinuxRepoFanout = linuxRepoFanout;
export type ILinuxRepoFanout = LinuxRepoFanout;

/**
 * Where the third-party audit is published. A claim nobody can check is a
 * claim, not evidence.
 */
export interface ThirdPartySecurityAuditPublisher {
  readonly reportUrl: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const thirdPartySecurityAuditPublisher: ThirdPartySecurityAuditPublisher = Object.freeze({
  reportUrl: "https://trust.circle.ai/audit",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultThirdPartySecurityAuditPublisher = thirdPartySecurityAuditPublisher;
export type IThirdPartySecurityAuditPublisher = ThirdPartySecurityAuditPublisher;

/**
 * Certifications held.
 */
export interface ComplianceCertifications {
  readonly certifications: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const complianceCertifications: ComplianceCertifications = Object.freeze({
  certifications: Object.freeze(["SOC 2 Type II", "ISO 27001", "ISO 27701"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultComplianceCertifications = complianceCertifications;
export type IComplianceCertifications = ComplianceCertifications;

/**
 * Where to report a vulnerability, and on what platform.
 */
export interface BugBountyChannel {
  readonly platform: string;
  readonly submissionUrl: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const bugBountyChannel: BugBountyChannel = Object.freeze({
  platform: "HackerOne",
  submissionUrl: "https://h1.com/circleai",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultBugBountyChannel = bugBountyChannel;
export type IBugBountyChannel = BugBountyChannel;

/**
 * Privacy laws this is built to comply with. POPIA is second because it is
 * the one that governs most of the people using it.
 */
export interface PrivacyRegulationCompliance {
  readonly laws: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const privacyRegulationCompliance: PrivacyRegulationCompliance = Object.freeze({
  laws: Object.freeze(["GDPR", "POPIA", "CCPA", "LGPD"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultPrivacyRegulationCompliance = privacyRegulationCompliance;
export type IPrivacyRegulationCompliance = PrivacyRegulationCompliance;

/**
 * A reproducible build and a source URL: the two things that let somebody
 * CHECK the privacy claim instead of believing it.
 */
export interface VerifiablePrivacyProof {
  readonly buildIsReproducible: boolean;
  readonly sourceUrl: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const verifiablePrivacyProof: VerifiablePrivacyProof = Object.freeze({
  buildIsReproducible: true,
  sourceUrl: "https://github.com/bhengubv/CircleAI",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultVerifiablePrivacyProof = verifiablePrivacyProof;
export type IVerifiablePrivacyProof = VerifiablePrivacyProof;

/**
 * What a plugin author keeps. Verified-safe plugins take a smaller share
 * because verification costs something and somebody has to pay for it.
 */
export interface PluginMarketplaceRevenueShare {
  readonly authorShare: number;
  readonly verifiedSafeShare: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const pluginMarketplaceRevenueShare: PluginMarketplaceRevenueShare = Object.freeze({
  authorShare: 0.7,
  verifiedSafeShare: 0.5,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultPluginMarketplaceRevenueShare = pluginMarketplaceRevenueShare;
export type IPluginMarketplaceRevenueShare = PluginMarketplaceRevenueShare;

/**
 * What a carrier takes on a preload.
 */
export interface CarrierRevenueShare {
  readonly carrierShare: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const carrierRevenueShare: CarrierRevenueShare = Object.freeze({
  carrierShare: 0.25,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCarrierRevenueShare = carrierRevenueShare;
export type ICarrierRevenueShare = CarrierRevenueShare;

/**
 * South African banks and wallets with a connector.
 */
export interface SaServiceConnectors {
  readonly banks: readonly string[];
  readonly wallets: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const saServiceConnectors: SaServiceConnectors = Object.freeze({
  banks: Object.freeze(["Capitec", "FNB", "Standard", "Absa", "Nedbank"]),
  wallets: Object.freeze(["PayFast", "SnapScan"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultSaServiceConnectors = saServiceConnectors;
export type ISaServiceConnectors = SaServiceConnectors;

/**
 * Cross-border corridors money can move along.
 */
export interface CrossBorderCorridors {
  readonly corridors: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const crossBorderCorridors: CrossBorderCorridors = Object.freeze({
  corridors: Object.freeze(["SADC", "ECOWAS", "EAC"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCrossBorderCorridors = crossBorderCorridors;
export type ICrossBorderCorridors = CrossBorderCorridors;

/**
 * Mail providers with a connector. IMAP is LAST and is the one that means
 * somebody with an unlisted provider is not shut out.
 */
export interface EmailConnectorRegistry {
  readonly providers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const emailConnectorRegistry: EmailConnectorRegistry = Object.freeze({
  providers: Object.freeze(["Gmail", "Outlook", "iCloud", "ProtonMail", "Yandex", "Yahoo", "IMAP"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultEmailConnectorRegistry = emailConnectorRegistry;
export type IEmailConnectorRegistry = EmailConnectorRegistry;

/**
 * Calendar providers. CalDAV plays the same role IMAP does above.
 */
export interface CalendarConnectorRegistry {
  readonly providers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const calendarConnectorRegistry: CalendarConnectorRegistry = Object.freeze({
  providers: Object.freeze(["Google", "Outlook", "Apple", "Yahoo", "CalDAV"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCalendarConnectorRegistry = calendarConnectorRegistry;
export type ICalendarConnectorRegistry = CalendarConnectorRegistry;

/**
 * CRMs with a connector.
 */
export interface CrmConnectorRegistry {
  readonly providers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const crmConnectorRegistry: CrmConnectorRegistry = Object.freeze({
  providers: Object.freeze(["HubSpot", "Salesforce", "Pipedrive", "Zoho", "Bitrix"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCrmConnectorRegistry = crmConnectorRegistry;
export type ICrmConnectorRegistry = CrmConnectorRegistry;

/**
 * Accounting packages with a connector.
 */
export interface AccountingConnectorRegistry {
  readonly providers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const accountingConnectorRegistry: AccountingConnectorRegistry = Object.freeze({
  providers: Object.freeze(["Xero", "Sage", "QuickBooks", "Wave", "Manager.io"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultAccountingConnectorRegistry = accountingConnectorRegistry;
export type IAccountingConnectorRegistry = AccountingConnectorRegistry;

/**
 * Open-banking rails, by jurisdiction. Named by STANDARD rather than by
 * bank, because the standard is what a connector actually speaks.
 */
export interface BankingConnectorRegistry {
  readonly providers: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const bankingConnectorRegistry: BankingConnectorRegistry = Object.freeze({
  providers: Object.freeze(["open-banking-ZA", "open-banking-NG", "open-banking-KE"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultBankingConnectorRegistry = bankingConnectorRegistry;
export type IBankingConnectorRegistry = BankingConnectorRegistry;

/**
 * KaiOS is compiled for. A feature phone is still a phone.
 */
export interface KaiOsSupport {
  readonly isCompiled: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const kaiOsSupport: KaiOsSupport = Object.freeze({
  isCompiled: true,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultKaiOsSupport = kaiOsSupport;
export type IKaiOsSupport = KaiOsSupport;

/**
 * The RAM floor. Below this is not a target device; AT this it must still
 * work.
 */
export interface LowRamPhoneSupport {
  readonly floorMb: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const lowRamPhoneSupport: LowRamPhoneSupport = Object.freeze({
  floorMb: 512,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultLowRamPhoneSupport = lowRamPhoneSupport;
export type ILowRamPhoneSupport = LowRamPhoneSupport;

/**
 * The CPU floor. Optimising for a slow CPU is on by default, not a tuning
 * option: the devices this is for are the slow ones.
 */
export interface LowCpuOptimization {
  readonly floorMhz: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const lowCpuOptimization: LowCpuOptimization = Object.freeze({
  floorMhz: 600,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultLowCpuOptimization = lowCpuOptimization;
export type ILowCpuOptimization = LowCpuOptimization;

/**
 * SARB sandbox status. FALSE, and it stays false until it is not — a
 * regulatory claim defaulting to true is the one lie that ends a company.
 */
export interface SarbSandboxStatus {
  readonly approved: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const sarbSandboxStatus: SarbSandboxStatus = Object.freeze({
  approved: false,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultSarbSandboxStatus = sarbSandboxStatus;
export type ISarbSandboxStatus = SarbSandboxStatus;

/**
 * ICASA status. False for the same reason.
 */
export interface IcasaApprovalStatus {
  readonly approved: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const icasaApprovalStatus: IcasaApprovalStatus = Object.freeze({
  approved: false,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultIcasaApprovalStatus = icasaApprovalStatus;
export type IIcasaApprovalStatus = IcasaApprovalStatus;

/**
 * Jurisdictions with active regulator engagement.
 */
export interface GlobalRegulatorEngagement {
  readonly activeJurisdictions: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const globalRegulatorEngagement: GlobalRegulatorEngagement = Object.freeze({
  activeJurisdictions: Object.freeze(["ZA", "NG", "KE", "US", "CA", "UK", "EU"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultGlobalRegulatorEngagement = globalRegulatorEngagement;
export type IGlobalRegulatorEngagement = GlobalRegulatorEngagement;

/**
 * Invoice tax schemes that can be issued under.
 */
export interface TaxInvoiceRegistry {
  readonly schemes: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const taxInvoiceRegistry: TaxInvoiceRegistry = Object.freeze({
  schemes: Object.freeze(["VAT", "GST", "Sales Tax", "DST"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultTaxInvoiceRegistry = taxInvoiceRegistry;
export type ITaxInvoiceRegistry = TaxInvoiceRegistry;

/**
 * The posture, in one line, because it is the sentence a regulator asks
 * for. Money is auditable; conversations are not, and cannot be made so
 * later.
 */
export interface LawfulInterceptCompliance {
  readonly posture: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const lawfulInterceptCompliance: LawfulInterceptCompliance = Object.freeze({
  posture: "Money decryptable to law, comms permanently blind",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultLawfulInterceptCompliance = lawfulInterceptCompliance;
export type ILawfulInterceptCompliance = LawfulInterceptCompliance;

/**
 * When the remote brain cannot be reached the local one takes over. A
 * device that stops working when a server does is not on-device.
 */
export interface BrainUnreachableMode {
  readonly localTakeoverEnabled: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const brainUnreachableMode: BrainUnreachableMode = Object.freeze({
  localTakeoverEnabled: true,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultBrainUnreachableMode = brainUnreachableMode;
export type IBrainUnreachableMode = BrainUnreachableMode;

/**
 * The share of requests that must be answerable with no internet at all.
 */
export interface NoInternetCacheTarget {
  readonly hitRateTarget: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const noInternetCacheTarget: NoInternetCacheTarget = Object.freeze({
  hitRateTarget: 0.8,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultNoInternetCacheTarget = noInternetCacheTarget;
export type INoInternetCacheTarget = NoInternetCacheTarget;

/**
 * What is given up first when the disk fills. 'nothing' is last and is
 * LITERAL: the assistant never deletes what somebody said to it.
 */
export interface StorageFullDegradationPolicy {
  readonly degradeOrder: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const storageFullDegradationPolicy: StorageFullDegradationPolicy = Object.freeze({
  degradeOrder: "cache > old-snapshots > chat-history > nothing",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultStorageFullDegradationPolicy = storageFullDegradationPolicy;
export type IStorageFullDegradationPolicy = StorageFullDegradationPolicy;

/**
 * Current disaster posture.
 */
export interface PublicDisasterMode {
  readonly currentState: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const publicDisasterMode: PublicDisasterMode = Object.freeze({
  currentState: "normal",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultPublicDisasterMode = publicDisasterMode;
export type IPublicDisasterMode = PublicDisasterMode;

/**
 * Minor units, not floats. Money in fractional binary is how a total stops
 * matching the sum of its parts.
 */
export interface SustainablePerUserCostMath {
  readonly monthlyRevenuePerUserMinor: number;
  readonly monthlyMarginalCostPerUserMinor: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const sustainablePerUserCostMath: SustainablePerUserCostMath = Object.freeze({
  monthlyRevenuePerUserMinor: 1900,
  monthlyMarginalCostPerUserMinor: 380,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultSustainablePerUserCostMath = sustainablePerUserCostMath;
export type ISustainablePerUserCostMath = SustainablePerUserCostMath;

/**
 * A single call may not cost more than this.
 */
export interface PerCallCostCeiling {
  readonly ceilingMicroUsd: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const perCallCostCeiling: PerCallCostCeiling = Object.freeze({
  ceilingMicroUsd: 400000,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultPerCallCostCeiling = perCallCostCeiling;
export type IPerCallCostCeiling = PerCallCostCeiling;

/**
 * What the free tier is allowed to cost, per user, per month.
 */
export interface FreeTierCostCapping {
  readonly monthlyCapMicroUsd: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const freeTierCostCapping: FreeTierCostCapping = Object.freeze({
  monthlyCapMicroUsd: 200000,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultFreeTierCostCapping = freeTierCostCapping;
export type IFreeTierCostCapping = FreeTierCostCapping;

/**
 * On-device first, always, unless something says otherwise.
 */
export interface LocalFirstRouting {
  readonly preferred: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const localFirstRouting: LocalFirstRouting = Object.freeze({
  preferred: true,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultLocalFirstRouting = localFirstRouting;
export type ILocalFirstRouting = LocalFirstRouting;

/**
 * The referral reward, in the local currency's minor unit.
 */
export interface ReferralProgramme {
  readonly rewardMinor: number;
  readonly currency: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const referralProgramme: ReferralProgramme = Object.freeze({
  rewardMinor: 1900,
  currency: "ZAR",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultReferralProgramme = referralProgramme;
export type IReferralProgramme = ReferralProgramme;

/**
 * How many people share one family plan.
 */
export interface FamilyAiSharing {
  readonly maxMembers: number;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const familyAiSharing: FamilyAiSharing = Object.freeze({
  maxMembers: 6,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultFamilyAiSharing = familyAiSharing;
export type IFamilyAiSharing = FamilyAiSharing;

/**
 * Federating with other providers is on. A network that only talks to
 * itself is a walled garden with extra steps.
 */
export interface CrossProviderFederation {
  readonly enabled: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const crossProviderFederation: CrossProviderFederation = Object.freeze({
  enabled: true,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultCrossProviderFederation = crossProviderFederation;
export type ICrossProviderFederation = CrossProviderFederation;

/**
 * The group shapes people actually organise into here. Stokvel FIRST,
 * because it is the one a foreign product always leaves out.
 */
export interface GroupNetworkEffects {
  readonly groupTypes: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const groupNetworkEffects: GroupNetworkEffects = Object.freeze({
  groupTypes: Object.freeze(["Stokvel", "Church", "Community"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultGroupNetworkEffects = groupNetworkEffects;
export type IGroupNetworkEffects = GroupNetworkEffects;

/**
 * The growth mechanic, stated plainly so it can be argued with.
 */
export interface UserGrowthFlywheel {
  readonly mechanic: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const userGrowthFlywheel: UserGrowthFlywheel = Object.freeze({
  mechanic: "user invites friend; both get a month free",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultUserGrowthFlywheel = userGrowthFlywheel;
export type IUserGrowthFlywheel = UserGrowthFlywheel;

/**
 * Who answers when a third party is harmed.
 */
export interface ThirdPartyHarmLiability {
  readonly framework: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const thirdPartyHarmLiability: ThirdPartyHarmLiability = Object.freeze({
  framework: "Operator-of-record indemnity backed by insurance pool",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultThirdPartyHarmLiability = thirdPartyHarmLiability;
export type IThirdPartyHarmLiability = ThirdPartyHarmLiability;

/**
 * COPPA and GDPR-K.
 */
export interface ChildProtectionMode {
  readonly coppaCompliant: boolean;
  readonly gdprKCompliant: boolean;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const childProtectionMode: ChildProtectionMode = Object.freeze({
  coppaCompliant: true,
  gdprKCompliant: true,
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultChildProtectionMode = childProtectionMode;
export type IChildProtectionMode = ChildProtectionMode;

/**
 * Accommodations that change when the assistant SPEAKS AT ALL.
 */
export interface ReligiousAccommodation {
  readonly supportedModes: readonly string[];
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const religiousAccommodation: ReligiousAccommodation = Object.freeze({
  supportedModes: Object.freeze(["prayer times", "Shabbat mode", "Eid silence"]),
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultReligiousAccommodation = religiousAccommodation;
export type IReligiousAccommodation = ReligiousAccommodation;

/**
 * The standard indigenous data is held to. CARE, not just FAIR: FAIR makes
 * data usable, CARE asks whose it is.
 */
export interface IndigenousDataSovereignty {
  readonly standard: string;
}

/** The default. Frozen: a rail a caller can mutate is not a rail. */
export const indigenousDataSovereignty: IndigenousDataSovereignty = Object.freeze({
  standard: "CARE Principles",
});

/** The C# spelling, kept so the two trees line up. */
export const DefaultIndigenousDataSovereignty = indigenousDataSovereignty;
export type IIndigenousDataSovereignty = IndigenousDataSovereignty;

/** Every constant rail, for a screen that lists what was decided. */
export const allConstantRails = Object.freeze({
  oemPreloadCatalog: oemPreloadCatalog,
  carrierPreloadCatalog: carrierPreloadCatalog,
  pwaFallback: pwaFallback,
  sideloadChannel: sideloadChannel,
  linuxRepoFanout: linuxRepoFanout,
  thirdPartySecurityAuditPublisher: thirdPartySecurityAuditPublisher,
  complianceCertifications: complianceCertifications,
  bugBountyChannel: bugBountyChannel,
  privacyRegulationCompliance: privacyRegulationCompliance,
  verifiablePrivacyProof: verifiablePrivacyProof,
  pluginMarketplaceRevenueShare: pluginMarketplaceRevenueShare,
  carrierRevenueShare: carrierRevenueShare,
  saServiceConnectors: saServiceConnectors,
  crossBorderCorridors: crossBorderCorridors,
  emailConnectorRegistry: emailConnectorRegistry,
  calendarConnectorRegistry: calendarConnectorRegistry,
  crmConnectorRegistry: crmConnectorRegistry,
  accountingConnectorRegistry: accountingConnectorRegistry,
  bankingConnectorRegistry: bankingConnectorRegistry,
  kaiOsSupport: kaiOsSupport,
  lowRamPhoneSupport: lowRamPhoneSupport,
  lowCpuOptimization: lowCpuOptimization,
  sarbSandboxStatus: sarbSandboxStatus,
  icasaApprovalStatus: icasaApprovalStatus,
  globalRegulatorEngagement: globalRegulatorEngagement,
  taxInvoiceRegistry: taxInvoiceRegistry,
  lawfulInterceptCompliance: lawfulInterceptCompliance,
  brainUnreachableMode: brainUnreachableMode,
  noInternetCacheTarget: noInternetCacheTarget,
  storageFullDegradationPolicy: storageFullDegradationPolicy,
  publicDisasterMode: publicDisasterMode,
  sustainablePerUserCostMath: sustainablePerUserCostMath,
  perCallCostCeiling: perCallCostCeiling,
  freeTierCostCapping: freeTierCostCapping,
  localFirstRouting: localFirstRouting,
  referralProgramme: referralProgramme,
  familyAiSharing: familyAiSharing,
  crossProviderFederation: crossProviderFederation,
  groupNetworkEffects: groupNetworkEffects,
  userGrowthFlywheel: userGrowthFlywheel,
  thirdPartyHarmLiability: thirdPartyHarmLiability,
  childProtectionMode: childProtectionMode,
  religiousAccommodation: religiousAccommodation,
  indigenousDataSovereignty: indigenousDataSovereignty,
});
