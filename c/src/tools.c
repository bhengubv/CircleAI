/*
 * tools.c — ToolDefinition, ToolInvocation, ToolResult are pure value types.
 * No logic is required beyond what the headers declare.
 * Pure C11, no OS-specific headers.
 */

#include "circle_ai/tools.h"
