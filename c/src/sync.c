/*
 * sync.c — SyncDelta and ISyncChannel are pure value / callback types.
 * No logic is required beyond what the headers declare.
 * Pure C11, no OS-specific headers.
 */

#include "circle_ai/sync.h"
