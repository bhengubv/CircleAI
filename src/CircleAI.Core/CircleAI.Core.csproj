<Project Sdk="Microsoft.NET.Sdk">

  <!-- ========== NUGET IDENTITY ========== -->
  <PropertyGroup>
    <PackageId>CircleAI.Core</PackageId>
    <Version>3.3.0</Version><Description>On-device AI core for Circle — 2.0.2 is a no-API-change refresh aligned with the 2.0.2 SDK bundle (skill-pack auto-import). Carries forward 2.0.0's RT-08 fallback chain stamping and RT-04 brownout sizing on ModelEntry.</Description>
  </PropertyGroup>

  <PropertyGroup>
		<TargetFrameworks>net9.0;net10.0</TargetFrameworks>
		<Nullable>enable</Nullable>
		<ImplicitUsings>enable</ImplicitUsings>
	</PropertyGroup>

	<ItemGroup>
		<!-- Legacy file kept for callers that load it by name; the canonical resource
		     consumed by ModelRegistryService is Models\embedded_registry.json. -->
		<EmbeddedResource Include="registry.json" />
		<EmbeddedResource Include="Models\embedded_registry.json" />
		<!-- The MMS voice sidecars. 91 KB of JSON that the registry used to pin as
		     REMOTE bundle files; 43 of the 47 were 404 because the bytes were
		     generated once and lost. They are our own work product and smaller than
		     a single icon, so they ship in the assembly and the download step
		     satisfies them locally instead of over the network. -->
		<EmbeddedResource Include="Models\VoiceConfigs\*.json" />
	</ItemGroup>

	<ItemGroup>
		<!-- 1.1.0 — CircleAIComponentBase + LoggerAuditLog need ILogger.
		     Abstractions-only so consumers choose their own logging backend. -->
		<PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="9.0.0" />
	</ItemGroup>

	<PropertyGroup>
		<EnableRuntimeIdentifierChecking>false</EnableRuntimeIdentifierChecking>
		<PublishReadyToRun>false</PublishReadyToRun>
		<StripSymbols>true</StripSymbols>
	</PropertyGroup>
</Project>
