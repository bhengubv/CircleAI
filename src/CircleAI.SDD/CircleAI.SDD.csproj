<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>CircleAI.SDD</PackageId>
    <Version>2.8.0</Version>
    <Description>(2.7.0) Spec-Driven Development contracts — github/spec-kit pattern-port. ISpecificationStore + ISpecificationValidator + ISpecToScaffold codegen hook. Lets a host hand the AI a spec document and get a compilable scaffold back. Null impls degrade to no-op. Real backends in 2.7.1.</Description>
    <Authors>The Geek Network</Authors>
    <PackageTags>ai;circle-ai;sdd;spec-kit;codegen;scaffolding</PackageTags>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFrameworks>net9.0;net10.0</TargetFrameworks>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\CircleAI.Core\CircleAI.Core.csproj" />
  </ItemGroup>

</Project>
